import test from'node:test';import assert from'node:assert/strict';import{extractSemanticSpans,selectRedactionSpans}from'../src/semantic-spans.js';
const text=`Jan Kowalski mieszka w Poznaniu przy ulicy Długiej 12.\nLekarz rozpoznał nadciśnienie i zalecił codzienne leczenie.\nWynagrodzenie pracownika wynosi 12 500 zł brutto.`;
const semantic=[{type:'identity',value:'Jan Kowalski mieszka w Poznaniu przy ulicy Długiej 12.',start:0,end:57,line:1,score:.9,source:'semantic-model'},{type:'medical',value:'Lekarz rozpoznał nadciśnienie i zalecił codzienne leczenie.',start:58,end:120,line:2,score:.9,source:'semantic-model'},{type:'financial',value:'Wynagrodzenie pracownika wynosi 12 500 zł brutto.',start:121,end:text.length,line:3,score:.9,source:'semantic-model'}];
test('extracts precise spans instead of redacting full lines',()=>{const spans=extractSemanticSpans(text,semantic);assert.ok(spans.some(x=>x.value==='Jan Kowalski'&&x.mask==='[PERSON]'));assert.ok(spans.some(x=>x.value==='Poznaniu'&&x.mask==='[CITY]'));assert.ok(spans.some(x=>x.value.includes('Długiej 12')&&x.mask==='[ADDRESS]'));assert.ok(spans.some(x=>x.value.includes('nadciśnienie')&&x.mask==='[MEDICAL_INFO]'));assert.ok(spans.some(x=>x.value.includes('12 500 zł brutto')&&x.mask==='[AMOUNT]'));assert.equal(selectRedactionSpans([...semantic,...spans]).some(x=>x.source==='semantic-model'),false)});