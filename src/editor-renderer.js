import { bilinearPoint, perspectiveQuad } from './editor-canvas-geometry.js';
import { resampleStroke } from './editor-paint.js';
import { rasterizeSelection } from './editor-selection.js';

export const COMPOSITE_OPERATIONS = Object.freeze({ normal: 'source-over', multiply: 'multiply', screen: 'screen', overlay: 'overlay' });
export function resolveCompositeOperation(blendMode) { return COMPOSITE_OPERATIONS[blendMode] ?? COMPOSITE_OPERATIONS.normal; }
export function buildRenderPlan(document) { const plan=[]; const visit=(layers,inherited={})=>{for(const layer of layers){if(!layer.visible||layer.opacity<=0)continue;if(layer.type==='group'){visit(layer.children,{opacity:(inherited.opacity??1)*layer.opacity,groupPath:[...(inherited.groupPath??[]),layer.id]});continue}plan.push({layer,layerId:layer.id,type:layer.type,opacity:(inherited.opacity??1)*layer.opacity,blendMode:layer.blendMode,compositeOperation:resolveCompositeOperation(layer.blendMode),groupId:inherited.groupPath?.at(-1)??null,groupPath:inherited.groupPath??[]})}};visit(document.layers);return plan; }

export class CanvasDocumentRenderer {
  constructor({canvas,createCanvas=defaultCreateCanvas,assetResolver=null}={}) { if(!canvas?.getContext)throw new TypeError('Renderer wymaga głównego canvasa.');this.canvas=canvas;this.createCanvas=createCanvas;this.assetResolver=assetResolver;this.paintCache=new Map(); }
  resize(width,height){if(this.canvas.width!==width)this.canvas.width=width;if(this.canvas.height!==height)this.canvas.height=height}
  render(document,options={}){this.resize(document.width,document.height);const context=this.canvas.getContext('2d');if(!context)throw new Error('Nie można uzyskać kontekstu 2D.');context.save?.();context.setTransform?.(1,0,0,1,0,0);context.globalAlpha=1;context.globalCompositeOperation='source-over';if(options.clear!==false)context.clearRect(0,0,document.width,document.height);const renderedLayerIds=[];this.renderLayerCollection(document.layers,document,context,renderedLayerIds);context.restore?.();return{plan:buildRenderPlan(document),renderedLayerIds}}
  renderLayerCollection(layers,document,context,renderedLayerIds){const renderedById=new Map;let previousBaseId=null;for(const layer of layers){if(!layer.visible||layer.opacity<=0)continue;const layerCanvas=this.renderLayer(layer,document,renderedLayerIds);if(!layerCanvas)continue;let composited=layerCanvas;if(layer.clipping?.enabled){const baseId=layer.clipping.baseLayerId??previousBaseId,baseCanvas=baseId?renderedById.get(baseId):null;if(baseCanvas)composited=this.applyAlphaSource(layerCanvas,baseCanvas,false,1)}else previousBaseId=layer.id;context.save?.();context.globalAlpha=layer.opacity;context.globalCompositeOperation=resolveCompositeOperation(layer.blendMode);context.drawImage(composited,0,0);context.restore?.();renderedById.set(layer.id,composited);renderedLayerIds.push(layer.id)}return renderedById}
  renderLayer(layer,document,renderedLayerIds=[]){let canvas=this.createCanvas(document.width,document.height);const context=canvas.getContext('2d');if(!context)return null;if(layer.type==='group'){this.renderLayerCollection(layer.children,document,context,renderedLayerIds);if(!isIdentityTransform(layer.transform)){const transformed=this.createCanvas(document.width,document.height),transformedContext=transformed.getContext('2d');transformedContext.save?.();applyTransform(transformedContext,layer.transform);transformedContext.drawImage(canvas,0,0);transformedContext.restore?.();canvas=transformed}}else{context.save?.();applyTransform(context,layer.transform);this.drawLayerContent(context,layer,document);context.restore?.()}if(layer.mask?.enabled){if(layer.mask.assetId){const maskAsset=this.resolveAsset(layer.mask.assetId,document);if(maskAsset)canvas=this.applyAlphaSource(canvas,maskAsset,layer.mask.inverted,layer.mask.opacity)}const eraseStrokes=layer.mask.metadata?.eraseStrokes??[];if(eraseStrokes.length)canvas=this.applyEraseStrokes(canvas,eraseStrokes,document)}return canvas}
  drawLayerContent(context,layer,document){if(layer.type==='raster'){const source=this.resolveAsset(layer.content.assetId,document);if(!source)return;const width=layer.content.width||source.width||document.width,height=layer.content.height||source.height||document.height,interpolation=layer.metadata?.interpolation??document.metadata?.interpolation??'high';context.imageSmoothingEnabled=interpolation!=='nearest';if(context.imageSmoothingEnabled)context.imageSmoothingQuality=['low','medium','high'].includes(interpolation)?interpolation:'high';const perspectiveX=Number(layer.transform?.perspectiveX)||0,perspectiveY=Number(layer.transform?.perspectiveY)||0;if(perspectiveX||perspectiveY)drawPerspectiveImage(context,source,width,height,perspectiveX,perspectiveY);else context.drawImage(source,0,0,width,height);return}if(layer.type==='paint'){this.drawPaintContent(context,layer,document);return}if(layer.type==='text'){const content=layer.content;context.fillStyle=content.color;context.font=`${content.fontWeight} ${content.fontSize}px ${content.fontFamily}`;context.textAlign=content.align;context.textBaseline='top';if(content.maxWidth>0)context.fillText(content.text,0,0,content.maxWidth);else context.fillText(content.text,0,0);return}if(layer.type==='shape')drawShape(context,layer.content)}
  drawPaintContent(context,layer,document){const width=layer.content.width||document.width,height=layer.content.height||document.height,fills=layer.content.fills??[],strokes=layer.content.strokes??[],byId=new Map([...fills,...strokes].map(operation=>[operation.id,operation])),orderedIds=(layer.content.operationOrder??[]).filter(id=>byId.has(id));for(const operation of[...fills,...strokes])if(!orderedIds.includes(operation.id))orderedIds.push(operation.id);let cache=this.paintCache.get(layer.id);const reusable=cache&&cache.width===width&&cache.height===height&&cache.operationIds.every((id,index)=>orderedIds[index]===id)&&cache.operationIds.length<=orderedIds.length;if(!reusable){cache={canvas:this.createCanvas(width,height),width,height,operationIds:[]};cache.canvas.getContext('2d')?.clearRect?.(0,0,width,height)}const cacheContext=cache.canvas.getContext('2d');for(let index=cache.operationIds.length;index<orderedIds.length;index+=1){const operation=byId.get(orderedIds[index]);if(operation?.points)this.drawPaintStroke(cacheContext,operation,width,height);else if(operation)this.drawPaintFill(cacheContext,operation,width,height);cache.operationIds.push(orderedIds[index])}this.paintCache.set(layer.id,cache);context.drawImage(cache.canvas,0,0)}
  drawPaintFill(context,fill,width,height){const operation=this.createCanvas(width,height),operationContext=operation.getContext('2d');operationContext.save?.();operationContext.globalAlpha=Math.min(1,Math.max(0,Number(fill.opacity)||0));if(fill.type==='radial'){const radius=Math.max(1,Math.hypot(fill.end.x-fill.start.x,fill.end.y-fill.start.y)),gradient=operationContext.createRadialGradient?.(fill.start.x,fill.start.y,0,fill.start.x,fill.start.y,radius);if(gradient){gradient.addColorStop(0,fill.colorA);gradient.addColorStop(1,fill.colorB);operationContext.fillStyle=gradient}else operationContext.fillStyle=fill.colorA}else if(fill.type==='linear'){const gradient=operationContext.createLinearGradient?.(fill.start.x,fill.start.y,fill.end.x,fill.end.y);if(gradient){gradient.addColorStop(0,fill.colorA);gradient.addColorStop(1,fill.colorB);operationContext.fillStyle=gradient}else operationContext.fillStyle=fill.colorA}else operationContext.fillStyle=fill.color;operationContext.fillRect?.(0,0,width,height);operationContext.restore?.();context.drawImage(this.applySelection(operation,fill.selection,width,height),0,0)}
  drawPaintStroke(context,stroke,width,height){const operation=this.createCanvas(width,height);drawBrushStroke(operation.getContext('2d'),stroke);context.drawImage(this.applySelection(operation,stroke.selection,width,height),0,0)}
  applySelection(source,selection,width,height){if(!selection?.entries?.length)return source;const mask=rasterizeSelection(selection,width,height),maskCanvas=this.createCanvas(width,height),context=maskCanvas.getContext('2d');if(!context?.createImageData||!context?.putImageData)return source;const imageData=context.createImageData(width,height);for(let index=0;index<mask.length;index+=1){const offset=index*4;imageData.data[offset]=imageData.data[offset+1]=imageData.data[offset+2]=255;imageData.data[offset+3]=mask[index]}context.putImageData(imageData,0,0);return this.applyAlphaSource(source,maskCanvas,false,1)}
  applyEraseStrokes(source,strokes,document){const output=this.createCanvas(source.width,source.height),context=output.getContext('2d');context.drawImage(source,0,0);for(const stroke of strokes){const operation=this.createCanvas(document.width,document.height);drawBrushStroke(operation.getContext('2d'),{...stroke,color:'#000000'});const selected=this.applySelection(operation,stroke.selection,document.width,document.height);context.save?.();context.globalCompositeOperation='destination-out';context.drawImage(selected,0,0);context.restore?.()}return output}
  resolveAsset(assetId,document){if(!assetId)return null;if(this.assetResolver)return this.assetResolver(assetId,document);return document.getRuntimeAsset?.(assetId)??null}
  applyAlphaSource(source,maskSource,inverted=false,opacity=1){const output=this.createCanvas(source.width,source.height),context=output.getContext('2d');context.drawImage(source,0,0);context.save?.();context.globalCompositeOperation=inverted?'destination-out':'destination-in';context.globalAlpha=Math.min(1,Math.max(0,Number(opacity)||0));context.drawImage(maskSource,0,0,source.width,source.height);context.restore?.();return output}
}

export function applyTransform(context,transform={}){const x=Number(transform.x)||0,y=Number(transform.y)||0,originX=Number(transform.originX)||0,originY=Number(transform.originY)||0,rotation=(Number(transform.rotation)||0)*Math.PI/180,scaleX=Number.isFinite(Number(transform.scaleX))?Number(transform.scaleX):1,scaleY=Number.isFinite(Number(transform.scaleY))?Number(transform.scaleY):1,skewX=(Number(transform.skewX)||0)*Math.PI/180,skewY=(Number(transform.skewY)||0)*Math.PI/180;context.translate?.(x+originX,y+originY);if(rotation)context.rotate?.(rotation);if((skewX||skewY)&&context.transform)context.transform(1,Math.tan(skewY),Math.tan(skewX),1,0,0);context.scale?.(scaleX,scaleY);context.translate?.(-originX,-originY)}
function isIdentityTransform(transform={}){return!Number(transform.x)&&!Number(transform.y)&&!Number(transform.rotation)&&!Number(transform.skewX)&&!Number(transform.skewY)&&!Number(transform.perspectiveX)&&!Number(transform.perspectiveY)&&(transform.scaleX??1)===1&&(transform.scaleY??1)===1}
function drawShape(context,content){context.save?.();if(content.shadowBlur>0||content.shadowOffsetX||content.shadowOffsetY){context.shadowColor=content.shadowColor||'rgba(0,0,0,.35)';context.shadowBlur=content.shadowBlur||0;context.shadowOffsetX=content.shadowOffsetX||0;context.shadowOffsetY=content.shadowOffsetY||0}context.beginPath?.();if(content.shape==='ellipse')context.ellipse?.(content.width/2,content.height/2,content.width/2,content.height/2,0,0,Math.PI*2);else if(content.shape==='line'||content.shape==='arrow'){context.moveTo?.(0,0);context.lineTo?.(content.width,content.height);if(content.shape==='arrow'){const angle=Math.atan2(content.height,content.width),size=Math.max(8,content.strokeWidth*4);context.moveTo?.(content.width,content.height);context.lineTo?.(content.width-Math.cos(angle-Math.PI/6)*size,content.height-Math.sin(angle-Math.PI/6)*size);context.moveTo?.(content.width,content.height);context.lineTo?.(content.width-Math.cos(angle+Math.PI/6)*size,content.height-Math.sin(angle+Math.PI/6)*size)}}else if(content.radius>0&&context.roundRect)context.roundRect(0,0,content.width,content.height,content.radius);else context.rect?.(0,0,content.width,content.height);if(content.fill){context.fillStyle=content.fill;context.fill?.()}if(content.stroke&&content.strokeWidth>0){context.strokeStyle=content.stroke;context.lineWidth=content.strokeWidth;context.stroke?.()}context.restore?.()}
export function drawPerspectiveImage(context,source,width,height,perspectiveX=0,perspectiveY=0,subdivisions=12){const columns=Math.max(2,Math.min(32,Math.round(subdivisions))),rows=columns,quad=perspectiveQuad(width,height,perspectiveX,perspectiveY),sourceWidth=Number(source.width||source.naturalWidth)||width,sourceHeight=Number(source.height||source.naturalHeight)||height;for(let row=0;row<rows;row+=1){const v0=row/rows,v1=(row+1)/rows;for(let column=0;column<columns;column+=1){const u0=column/columns,u1=(column+1)/columns,s00={x:u0*sourceWidth,y:v0*sourceHeight},s10={x:u1*sourceWidth,y:v0*sourceHeight},s11={x:u1*sourceWidth,y:v1*sourceHeight},s01={x:u0*sourceWidth,y:v1*sourceHeight},d00=bilinearPoint(quad,u0,v0),d10=bilinearPoint(quad,u1,v0),d11=bilinearPoint(quad,u1,v1),d01=bilinearPoint(quad,u0,v1);drawTexturedTriangle(context,source,[s00,s10,s11],[d00,d10,d11]);drawTexturedTriangle(context,source,[s00,s11,s01],[d00,d11,d01])}}}
function drawTexturedTriangle(context,source,sourceTriangle,destinationTriangle){const matrix=affineFromTriangles(sourceTriangle,destinationTriangle);if(!matrix)return;context.save?.();context.beginPath?.();context.moveTo?.(destinationTriangle[0].x,destinationTriangle[0].y);context.lineTo?.(destinationTriangle[1].x,destinationTriangle[1].y);context.lineTo?.(destinationTriangle[2].x,destinationTriangle[2].y);context.closePath?.();context.clip?.();context.transform?.(...matrix);context.drawImage(source,0,0);context.restore?.()}
function affineFromTriangles(source,destination){const[s0,s1,s2]=source,determinant=s0.x*(s1.y-s2.y)+s1.x*(s2.y-s0.y)+s2.x*(s0.y-s1.y);if(Math.abs(determinant)<1e-10)return null;const solve=values=>[(values[0]*(s1.y-s2.y)+values[1]*(s2.y-s0.y)+values[2]*(s0.y-s1.y))/determinant,(values[0]*(s2.x-s1.x)+values[1]*(s0.x-s2.x)+values[2]*(s1.x-s0.x))/determinant,(values[0]*(s1.x*s2.y-s2.x*s1.y)+values[1]*(s2.x*s0.y-s0.x*s2.y)+values[2]*(s0.x*s1.y-s1.x*s0.y))/determinant],[a,c,e]=solve(destination.map(point=>point.x)),[b,d,f]=solve(destination.map(point=>point.y));return[a,b,c,d,e,f]}
function drawBrushStroke(context,stroke){if(!context||!stroke?.points?.length)return;const size=Math.max(1,Number(stroke.size)||1),dabs=resampleStroke(stroke.points,Math.max(.25,size*Math.max(.02,Number(stroke.spacing)||.2)));context.save?.();context.globalAlpha=Math.min(1,Math.max(0,Number(stroke.opacity)||0));for(const point of dabs){const radius=Math.max(.5,size*(Number(point.pressure)||1)/2);context.beginPath?.();if(context.createRadialGradient&&Number(stroke.hardness)<.999){const hardness=Math.min(1,Math.max(0,Number(stroke.hardness)||0)),gradient=context.createRadialGradient(point.x,point.y,radius*hardness,point.x,point.y,radius);gradient.addColorStop(0,stroke.color);gradient.addColorStop(Math.max(hardness,.001),stroke.color);gradient.addColorStop(1,transparentColor(stroke.color));context.fillStyle=gradient}else context.fillStyle=stroke.color;context.arc?.(point.x,point.y,radius,0,Math.PI*2);context.fill?.()}context.restore?.()}
function transparentColor(color){if(/^#[0-9a-f]{6}$/i.test(color))return`${color}00`;if(/^#[0-9a-f]{3}$/i.test(color))return`${color[0]}${color[1]}${color[1]}${color[2]}${color[2]}${color[3]}${color[3]}00`;return'rgba(0,0,0,0)'}
function defaultCreateCanvas(width,height){if(typeof OffscreenCanvas!=='undefined')return new OffscreenCanvas(width,height);if(typeof document!=='undefined'){const canvas=document.createElement('canvas');canvas.width=width;canvas.height=height;return canvas}throw new Error('Brak implementacji canvasa. Przekaż createCanvas do renderera.')}
